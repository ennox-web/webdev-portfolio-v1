export interface ProjectDataInterface {
    name: string;
    description: string;
    skills: {[id: string]: string[]};
    images: string[];
}

export default function ProjectBlock() {
    return (
        <div></div>
    )
}